#!/bin/sh
echo "------> Nothing to compile"

if [ -e "$ENV_DIR/DATABASE_URL" ]; then
echo "yahoo"
export ROCKET_DATABASES="{genapi_heroku_test={url=$DATABASE_URL}}"
fi
export ROCKET_PORT="$PORT"
echo $ROCKET_PORT
echo $ROCKET_DATABASES
exit 0
